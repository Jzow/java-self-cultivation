/**
 * package com.jameszow.iterator;
 *
 * 迭代器模式：
 *
 * 提供一个对象顺序访问聚合对象中的一些数据，而不暴露聚合对象内部，
 * 迭代器模式是一种对象行为模式。
 *
 * 迭代器模式已经被淘汰，java中已经把迭代器运用到各个聚集类（collection）中了。
 *
 * 使用场景：
 *
 * 当需要为聚合对象提供多种遍历方式时。
 * 当需要为遍历不同的聚合结构提供一个统一接口时。
 * 当访问一个聚合对象的内容无需暴露其内部细节的表示时。
 */
package com.jameszow.iterator;