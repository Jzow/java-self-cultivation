/**
 * package com.jameszow.memento;
 *
 * 备忘录模式：
 *
 * 备忘录模式又叫做快照或者token模式，其功能跟快照备份类似，是一种对象行为模式，
 * 在备忘录模式里 一个备忘录就是一个对象，它的本质是保存和恢复状态。
 * 设计的意图在于不破坏封装的前提下，捕获一个对象内部状态，并在对象之外保存这个状态，这样就可以恢复到之前保存的状态。
 *
 * 使用场景：
 *
 * 如保存一个对象在某一刻的状态，方便在以后使用，可以把对象恢复到先前状态
 */
package com.jameszow.memento;