/**
 * package com.jameszow.composite;
 *
 * 组合模式：
 *
 * 将对象组合成树形结构，以表示"部分"-"整体"的层次结构
 * 组成的角色有三种：
 * 组合部件 是一个抽象的接口，表示根root
 * 叶子 在组合中表示子节点对象，这里表示文件，没有子节点
 * 合成部件 这里表示文件夹，还有子节点
 *
 * 使用场景：
 *
 * 当想表达对象的部分-整体的层次结构时。
 */
package com.jameszow.composite;